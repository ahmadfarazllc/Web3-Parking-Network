import { Metadata } from 'next';
import Collections from '../../../containers/Account/Collections';

export const metadata: Metadata = {
  title: 'Account - Collections'
};

export default Collections;
